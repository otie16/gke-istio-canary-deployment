apiVersion: networking.istio.io/v1
kind: DestinationRule
metadata:
  name: istio-app
spec:
  host: istio-app
  subsets:
    - name: stable
      labels:
        version: stable
    - name: canary
      labels:
        version: canary